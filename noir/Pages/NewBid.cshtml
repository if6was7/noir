@page
@model noir.Pages.NewBidModel
@using System.Linq
@functions {
    public class AuctionLot
    {
        public int Id { get; set; }
        public string Title { get; set; } = "";
        public int CurrentBid { get; set; }
        public int MinBid => (int)Math.Round(CurrentBid * 0.1);
        public int LotId => Id;
    }

    public AuctionLot[] GetAllLots()
    {
        return new[] {
            new AuctionLot { Id = 10, Title = "ancient marble bust",      CurrentBid = 85000  },
            new AuctionLot { Id = 11, Title = "viking iron sword",         CurrentBid = 32000  },
            new AuctionLot { Id = 12, Title = "dynasty ritual vessel",     CurrentBid = 120000 },
            new AuctionLot { Id = 13, Title = "gothic cathedral key",      CurrentBid = 15000  },
            new AuctionLot { Id = 14, Title = "ottoman silk tapestry",     CurrentBid = 47000  },
            new AuctionLot { Id = 15, Title = "venetian poison ring",      CurrentBid = 22000  },
            new AuctionLot { Id = 16, Title = "aztec obsidian blade",      CurrentBid = 68000  },
            new AuctionLot { Id = 17, Title = "czarist fabergé fragment",  CurrentBid = 310000 }
        };
    }

    public AuctionLot GetLot()
    {
        var all = GetAllLots();
        int.TryParse(Request.Query["id"], out int id);
        return all.FirstOrDefault(i => i.Id == id) ?? all[0];
    }
}
@{
    Layout = null;
    var lot = GetLot();
}
<!DOCTYPE html>
<html lang="ru">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>noir — place a bid</title>
    <link href="https://fonts.googleapis.com/css2?family=Sorts+Mill+Goudy:ital@0;1&family=Cinzel:wght@700&family=Montserrat:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg: #ffffff;
            --text: #000;
            --gray: #efefef;
            --purple: #4b0082;
            --accent-gray: #888;
            --accent-purple: #a855f7;
        }
        * { box-sizing: border-box; margin: 0; padding: 0; }
        html { background-color: #000; }
        body { background-color: var(--bg); color: var(--text); font-family: 'Goudy Old Style', serif; -webkit-font-smoothing: antialiased; min-height: 100vh; overscroll-behavior-y: none; }

        /* ── Header (идентичен всем остальным страницам) ── */
        header { background: #000; height: 60px; display: grid; grid-template-columns: 1fr auto 1fr; align-items: center; padding: 0 60px; position: sticky; top: 0; z-index: 1000; }
        .brand-wrapper { display: inline-flex; align-items: flex-end; gap: 5px; text-decoration: none; }
        .brand { font-family: 'Cinzel', serif; font-size: 2.8rem; letter-spacing: 5px; text-transform: lowercase; color: #fff; line-height: 1; }
        .plus-sticker { display: none; color: var(--accent-purple); font-family: 'Montserrat', sans-serif; font-size: 0.5rem; font-weight: 600; letter-spacing: 2px; text-transform: uppercase; line-height: 1; margin-bottom: 10px; }
        nav { display: flex; gap: 60px; }
        nav a { text-decoration: none; color: #fff; font-size: 1.5rem; opacity: 0.45; transition: 0.3s; }
        nav a:hover, nav a.active { opacity: 1; }
        .header-right { justify-self: end; display: flex; align-items: center; gap: 18px; }
        .header-avatar-wrap { display: flex; align-items: center; gap: 10px; cursor: pointer; }
        .header-avatar {
            width: 28px; height: 28px; border-radius: 50%;
            background: #1a1a1a; border: 1px solid #2a2a2a;
            display: flex; align-items: center; justify-content: center;
            font-size: 0.6rem; font-weight: 600; color: #fff;
            overflow: hidden; font-family: 'Cinzel', serif;
            transition: border-color 0.3s;
        }
        .header-avatar img { width: 100%; height: 100%; object-fit: cover; }
        .header-avatar.plus-ring { border-color: var(--accent-purple); }
        .header-username { font-family: 'Montserrat'; font-size: 0.65rem; letter-spacing: 2px; color: #fff; opacity: 0.5; text-transform: uppercase; }
        .logout-btn { font-family: 'Montserrat'; font-size: 0.6rem; letter-spacing: 2px; text-transform: uppercase; color: #fff; opacity: 0.3; background: none; border: none; cursor: pointer; transition: 0.2s; padding: 0; }
        .logout-btn:hover { opacity: 1; }

        /* ── Page content ── */
        .container { max-width: 900px; margin: 80px auto; padding: 0 40px; }

        .bid-header { border-left: 2px solid #000; padding-left: 30px; margin-bottom: 60px; }
        .bid-header-label { font-family: 'Montserrat'; font-size: 0.6rem; text-transform: uppercase; letter-spacing: 4px; opacity: 0.4; margin-bottom: 10px; }
        .bid-header h1 { font-family: 'Cinzel'; font-size: 2.8rem; letter-spacing: 4px; text-transform: lowercase; font-weight: normal; }

        .lot-summary {
            display: flex; justify-content: space-between; align-items: center;
            border: 1px solid #efefef; padding: 25px 30px; margin-bottom: 50px;
        }
        .lot-summary-title { font-size: 1.1rem; text-transform: uppercase; font-family: 'Montserrat'; font-weight: 300; letter-spacing: 3px; opacity: 0.6; }
        .lot-summary-bid { text-align: right; }
        .lot-summary-bid-label { font-family: 'Montserrat'; font-size: 0.55rem; text-transform: uppercase; letter-spacing: 3px; opacity: 0.4; margin-bottom: 6px; }
        .lot-summary-bid-value { font-family: 'Montserrat'; font-size: 1.1rem; font-weight: 600; }

        .section-label { font-family: 'Montserrat'; font-size: 0.55rem; text-transform: uppercase; letter-spacing: 3px; color: var(--accent-gray); margin-bottom: 20px; display: block; }

        .payment-methods { display: flex; gap: 12px; margin-bottom: 50px; flex-wrap: wrap; }
        .pay-option { cursor: pointer; }
        .pay-option input { display: none; }
        .pay-label {
            display: flex; align-items: center; justify-content: center;
            width: 110px; height: 76px; border: 1px solid var(--gray);
            transition: border-color 0.3s; cursor: pointer;
        }
        .pay-label:hover { border-color: #aaa; }
        .pay-option input:checked + .pay-label { border-color: #000; outline: 1px solid #000; }
        .pay-img { width: 68px; height: 42px; object-fit: contain; image-rendering: -webkit-optimize-contrast; image-rendering: crisp-edges; }
        .balance-label { font-family: 'Montserrat'; font-size: 0.6rem; text-transform: uppercase; letter-spacing: 3px; font-weight: 500; opacity: 0.5; }

        .custom-pay-input { display: none; margin-bottom: 50px; }
        .custom-pay-input.visible { display: block; }

        .noir-input {
            width: 100%; border: none; border-bottom: 1px solid var(--gray); background: transparent;
            padding: 12px 0; font-family: 'Montserrat', sans-serif; font-size: 1.1rem;
            outline: none; transition: 0.3s; font-weight: 300; color: #000;
        }
        .noir-input:focus { border-bottom-color: #000; }
        .noir-input::placeholder { color: #ccc; }

        .bid-amount-wrap { margin-bottom: 40px; }
        .bid-amount-row {
            display: flex; align-items: baseline;
            border-bottom: 1px solid var(--gray); padding-bottom: 12px;
            gap: 10px; transition: 0.3s;
        }
        .bid-amount-row:focus-within { border-bottom-color: #000; }
        .currency-label { font-family: 'Montserrat'; font-size: 1.1rem; font-weight: 300; opacity: 0.4; }
        .bid-input {
            flex: 1; border: none; background: transparent;
            font-family: 'Montserrat', sans-serif; font-size: 2rem; font-weight: 300;
            outline: none; color: #000;
        }
        .bid-input::placeholder { color: #ddd; }
        .min-bid-note { font-family: 'Montserrat'; font-size: 0.6rem; text-transform: uppercase; letter-spacing: 2px; opacity: 0.35; margin-top: 12px; }

        .confirm-btn {
            width: 100%; height: 80px; background: #000; color: #fff; border: none;
            font-family: 'Montserrat'; font-size: 0.8rem; text-transform: uppercase; letter-spacing: 6px;
            cursor: pointer; transition: 0.5s; margin-bottom: 20px;
        }
        .confirm-btn:hover { background: var(--purple); }
        .confirm-note { font-family: 'Montserrat'; font-size: 0.55rem; text-transform: uppercase; letter-spacing: 2px; opacity: 0.3; text-align: center; }

        .footer { margin-top: 120px; border-top: 1px solid #000; padding: 60px 0; display: grid; grid-template-columns: 1fr auto 1fr; align-items: center; }
        .footer-brand { font-family: 'Cinzel', serif; font-size: 1.2rem; letter-spacing: 5px; text-transform: lowercase; }
        .footer-copy { font-family: 'Montserrat', sans-serif; font-size: 0.55rem; text-transform: uppercase; letter-spacing: 3px; opacity: 0.35; text-align: center; }
        .footer-tagline { font-family: 'Montserrat', sans-serif; font-size: 0.55rem; text-transform: uppercase; letter-spacing: 3px; opacity: 0.35; text-align: right; }
    </style>
</head>
<body>
    <header>
        <a href="/" class="brand-wrapper">
            <span class="brand">noir</span>
            <span id="headerPlus" class="plus-sticker">plus</span>
        </a>
        <nav>
            <a href="/">Catalog</a>
            <a href="/Auction" class="active">Auction</a>
            <a href="/Archive">Archive</a>
            <a href="/Upload">Upload</a>
        </nav>
        <div class="header-right">
            <div class="header-avatar-wrap" onclick="window.location.href='/Account'">
                <div class="header-avatar" id="headerAvatar"></div>
                <span class="header-username" id="headerUsername"></span>
            </div>
            <button class="logout-btn" onclick="logout()">exit</button>
        </div>
    </header>

    <main class="container">

        <div id="bidForm">
            <div class="bid-header">
                <div class="bid-header-label">Archive Ref: 00@lot.LotId — Live Auction</div>
                <h1>@lot.Title</h1>
            </div>

            <div class="lot-summary">
                <span class="lot-summary-title">Archive Ref: 00@lot.LotId</span>
                <div class="lot-summary-bid">
                    <div class="lot-summary-bid-label">Current bid</div>
                    <div class="lot-summary-bid-value">EUR @lot.CurrentBid.ToString("N0")</div>
                </div>
            </div>

            <span class="section-label">Payment method</span>
            <div class="payment-methods">
                <label class="pay-option">
                    <input type="radio" name="payment" value="paypal" checked>
                    <span class="pay-label">
                        <img class="pay-img" src="/images/paypal.jpg" alt="PayPal">
                    </span>
                </label>
                <label class="pay-option">
                    <input type="radio" name="payment" value="visa" onchange="hideCustom()">
                    <span class="pay-label">
                        <img class="pay-img" src="/images/visa.jpg" alt="Visa">
                    </span>
                </label>
                <label class="pay-option">
                    <input type="radio" name="payment" value="mastercard" onchange="hideCustom()">
                    <span class="pay-label">
                        <img class="pay-img" src="/images/mastercard.jpg" alt="Mastercard">
                    </span>
                </label>
                <label class="pay-option">
                    <input type="radio" name="payment" value="applepay" onchange="hideCustom()">
                    <span class="pay-label">
                        <img class="pay-img" src="/images/applepay.jpg" alt="Apple Pay">
                    </span>
                </label>
                <label class="pay-option">
                    <input type="radio" name="payment" value="balance" onchange="hideCustom()">
                    <span class="pay-label">
                        <span class="balance-label">Balance</span>
                    </span>
                </label>
                <label class="pay-option">
                    <input type="radio" name="payment" value="custom" onchange="showCustom()">
                    <span class="pay-label">
                        <span style="font-size: 2rem; opacity: 0.25; line-height: 1;">＋</span>
                    </span>
                </label>
            </div>

            <div class="custom-pay-input" id="customPayInput">
                <span class="section-label">Your payment method</span>
                <input type="text" class="noir-input" placeholder="Describe your payment method...">
            </div>

            <div class="bid-amount-wrap">
                <span class="section-label">Your bid</span>
                <div class="bid-amount-row">
                    <span class="currency-label">EUR</span>
                    <input type="number" class="bid-input" id="bidInput" placeholder="@((lot.CurrentBid + lot.MinBid).ToString())">
                </div>
                <div class="min-bid-note">Minimum bid: EUR @((lot.CurrentBid + lot.MinBid).ToString("N0"))</div>
            </div>

            <button class="confirm-btn" onclick="confirmBid(@lot.CurrentBid, @lot.MinBid)">Confirm Bid</button>
            <div class="confirm-note">By confirming you agree to our terms. All bids are binding.</div>
        </div>

        <footer class="footer">
            <span class="footer-brand">noir</span>
            <span class="footer-copy">© 2026 noir private collection. all rights reserved.</span>
            <span class="footer-tagline">objects of consequence</span>
        </footer>
    </main>

    <script>
        var LOT_ID = @lot.Id;
        var LOT_TITLE = "@lot.Title";

        // ── Header auth ──
        (function() {
            var userData = sessionStorage.getItem('noir_user');
            if (!userData) { window.location.href = '/Log_In'; return; }
            var user = JSON.parse(userData);
            var originalName = user.username || "";
            var clean = originalName.replace('@@', '');
            var initial = clean.charAt(0).toUpperCase() || 'U';
            var avatarPath = '/images/avatar/' + clean + '.jpg';

            var ha = document.getElementById('headerAvatar');
            var haImg = document.createElement('img');
            haImg.src = avatarPath;
            haImg.style.cssText = 'width:100%;height:100%;object-fit:cover;';
            haImg.onerror = function() { ha.removeChild(haImg); ha.innerText = initial; };
            ha.appendChild(haImg);

            document.getElementById('headerUsername').innerText = originalName;

            var low = originalName.toLowerCase();
            var isPlus = low.indexOf('if6was7') !== -1 || user.hasPlus === true || user.hasPlus === "true";
            if (isPlus) {
                document.getElementById('headerPlus').style.display = 'inline';
                ha.classList.add('plus-ring');
            }
        })();

        function logout() {
            sessionStorage.removeItem('noir_user');
            window.location.href = '/Log_In';
        }

        // ── Bid logic ──
        function showCustom() {
            document.getElementById('customPayInput').classList.add('visible');
        }
        function hideCustom() {
            document.getElementById('customPayInput').classList.remove('visible');
        }

        // FIX: redirect to Payment.cshtml with source=auction + lotId
        function confirmBid(currentBid, minBid) {
            var input = document.getElementById('bidInput');
            var val = parseInt(input.value);
            var minRequired = currentBid + minBid;

            if (!val || val < minRequired) {
                input.style.color = 'red';
                setTimeout(function() { input.style.color = ''; }, 1500);
                return;
            }

            var selectedPayment = document.querySelector('input[name="payment"]:checked').value;
            window.location.href = '/Payment?source=auction&method=' + encodeURIComponent(selectedPayment) + '&amount=' + val + '&lotId=' + LOT_ID;
        }
    </script>
</body>
</html>